stages:
  - build
  - test

# Builds dependencies, then builds the web components.
build:
  script:
    - npm ci
    - npm run build
  artifacts:
    paths:
      - './node_modules'
      - './build'

# Runs unit tests.
test:unit-tests:
  script: 
    - npm test
  needs:
    - build